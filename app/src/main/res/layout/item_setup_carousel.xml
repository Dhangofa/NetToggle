<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:paddingHorizontal="16dp"
    android:paddingVertical="10dp"
    android:id="@+id/carouselBackground"
    android:background="@drawable/shape_qs_hint_bg" 
    android:clipChildren="false"
    android:clipToPadding="false"
    android:gravity="center_vertical">
    
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:orientation="vertical">
        
        <TextView
            android:id="@+id/carouselTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary"
            android:textSize="16sp"
            android:textStyle="bold"
            android:text="New to NetToggle?" />
            
        <TextView
            android:id="@+id/carouselDesc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:layout_marginTop="4dp"
            android:text="Learn how to set up Execution Modes" />
            
        <LinearLayout
            android:id="@+id/carouselButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:background="@drawable/shape_pill_badge_bg"
            android:backgroundTint="@color/card_surface"
            android:gravity="center_vertical"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:elevation="2dp"
            android:layout_marginStart="2dp"
            android:layout_marginTop="6dp"
            android:layout_marginBottom="4dp"
            android:clickable="true"
            android:focusable="true">
            <TextView
                android:id="@+id/carouselButtonText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/exec_accent"
                android:textStyle="bold"
                android:textSize="12sp"
                android:text="Read Guide" />
            <ImageView
                android:id="@+id/carouselButtonIcon"
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:src="@drawable/ic_open_in_new"
                android:layout_marginStart="4dp"
                android:tint="@color/exec_accent" />
        </LinearLayout>
    </LinearLayout>
    
    <FrameLayout
        android:id="@+id/carouselIconContainer"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/shape_pill_badge_bg"
        android:backgroundTint="@color/card_surface"
        android:layout_marginStart="16dp">
        <ImageView
            android:id="@+id/carouselIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_terminal"
            android:tint="@color/exec_accent" />
    </FrameLayout>
</LinearLayout>
